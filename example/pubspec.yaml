name: flutter_launch_arguments_example
description: "Demonstrates how to use the flutter_launch_arguments plugin."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

  flutter_launch_arguments:
    path: ../

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true